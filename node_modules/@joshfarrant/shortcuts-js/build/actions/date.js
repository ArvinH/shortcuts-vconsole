"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../utils");
/**
 * @action Date
 * @section Content Types > Calendar > Dates
 * @icon Date
 *
 * Passes the specified date and time to the next action.
 *
 * ```js
 * date({
 *   use: 'Specified Date',
 *   date: 'Tomorrow',
 * });
 * ```
 */
const date = ({ use = 'Current Date', date = '29 June 2007', }) => ({
    WFWorkflowActionIdentifier: 'is.workflow.actions.date',
    WFWorkflowActionParameters: Object.assign({ WFDateActionMode: use }, (use === 'Specified Date' && { WFDateActionDate: date })),
});
exports.default = utils_1.withActionOutput(date);
//# sourceMappingURL=date.js.map